import { ValidateAlbumIdPipe } from './validate-album-id.pipe';

describe('ValidateAlbumIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidateAlbumIdPipe()).toBeDefined();
  });
});
